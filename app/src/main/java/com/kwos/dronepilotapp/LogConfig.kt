package com.kwos.dronepilotapp

object LogConfig {
    // Imposta su true per attivare i log, su false per disattivarli
    const val DEBUG_MODE = true  // Puoi anche usare BuildConfig.DEBUG per automatizzare in base alla modalità build
}